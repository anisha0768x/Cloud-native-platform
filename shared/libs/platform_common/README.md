# platform_common

Shared code is limited to cross-cutting concerns: typed settings, structured logs, JWT verification helpers, request IDs and event-envelope validation. Domain models and repositories stay inside the service that owns them.
