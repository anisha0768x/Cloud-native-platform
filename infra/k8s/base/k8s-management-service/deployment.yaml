apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-management-service
  namespace: platform
  labels:
    app: k8s-management-service
    part-of: cloud-native-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: k8s-management-service
  template:
    metadata:
      labels:
        app: k8s-management-service
    spec:
      serviceAccountName: k8s-management-service
      containers:
        - name: k8s-management-service
          image: k8s-management-service:latest  # replaced by CI with the built image digest — see .github/workflows/deploy.yml
          ports:
            - containerPort: 8004
          envFrom:
            - configMapRef:
                name: k8s-management-service-config
            - secretRef:
                name: k8s-management-service-secrets
          env:
            - name: DATABASE_URL
              value: "postgresql+asyncpg://platform:$(DB_PASSWORD)@postgres:5432/k8s_management_service"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: 8004
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
