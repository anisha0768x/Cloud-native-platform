apiVersion: apps/v1
kind: Deployment
metadata:
  name: traffic-prediction-service
  namespace: ai
  labels:
    app: traffic-prediction-service
    part-of: cloud-native-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: traffic-prediction-service
  template:
    metadata:
      labels:
        app: traffic-prediction-service
    spec:
      containers:
        - name: traffic-prediction-service
          image: traffic-prediction-service:latest  # replaced by CI with the built image digest — see .github/workflows/deploy.yml
          ports:
            - containerPort: 8006
          envFrom:
            - configMapRef:
                name: traffic-prediction-service-config
            - secretRef:
                name: traffic-prediction-service-secrets
          env:
            - name: DATABASE_URL
              value: "postgresql+asyncpg://platform:$(DB_PASSWORD)@postgres:5432/traffic_prediction_service"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: 8006
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
