apiVersion: v1
kind: Service
metadata:
  name: traffic-prediction-service
  namespace: ai
  labels:
    app: traffic-prediction-service
spec:
  selector:
    app: traffic-prediction-service
  ports:
    - port: 8006
      targetPort: 8006
  type: ClusterIP
