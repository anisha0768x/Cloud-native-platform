apiVersion: v1
kind: Service
metadata:
  name: monitoring-service
  namespace: monitoring
  labels:
    app: monitoring-service
spec:
  selector:
    app: monitoring-service
  ports:
    - port: 8002
      targetPort: 8002
  type: ClusterIP
