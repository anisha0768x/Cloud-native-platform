apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: platform
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - port: 8009
      targetPort: 8009
  type: ClusterIP
