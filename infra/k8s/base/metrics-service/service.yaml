apiVersion: v1
kind: Service
metadata:
  name: metrics-service
  namespace: data
  labels:
    app: metrics-service
spec:
  selector:
    app: metrics-service
  ports:
    - port: 8003
      targetPort: 8003
  type: ClusterIP
