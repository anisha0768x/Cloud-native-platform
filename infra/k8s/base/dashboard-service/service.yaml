apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: platform
  labels:
    app: dashboard-service
spec:
  selector:
    app: dashboard-service
  ports:
    - port: 8005
      targetPort: 8005
  type: ClusterIP
